---
import "../styles/global.css";
import { SITE_VERSION } from "../config";
---

<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Фронтовые Дневники ВОВ - Интерактивная карта</title>
    <meta name="description" content="Интерактивная карта дневников фронтовиков Великой Отечественной войны" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=PT+Serif:ital,wght@0,400;0,700;1,400&family=Roboto+Slab:wght@400;600;700&display=swap" rel="stylesheet" />
  </head>
  <body>
    <main class="page-shell">
      <section class="split hero-panel">
        <div class="hero-overlay">
          <p class="hero-kicker">Интерактивная карта</p>
          <h1 class="hero-title">Фронтовые дневники</h1>
          <p class="hero-subtitle">Великая Отечественная война 1941-1945</p>
          <a href="/map" class="cta-button">Перейти к карте</a>
        </div>
      </section>

      <section class="split info-panel">
        <div class="info-top">
          <div class="logo-wrap">
            <img src="/assets/logos/itmo-logo-white.png" alt="Университет ИТМО" class="logo-itmo" />
          </div>
          <a class="sources-button" href="/all-sources">
            <span class="sources-logo"></span>
            <span class="sources-text">Используемые источники</span>
          </a>
        </div>

        <div class="info-card">
          <p class="info-card-title">Только образовательные цели</p>
          <p class="info-card-body">
            Проект создан исключительно в образовательных целях. Является выполнением домашнего задания по дисциплине "Россия в истории современных международных отношений" от 29.11.2025. Информация будет постепенно дополняться.
          </p>
        </div>

        <div class="contacts-card">
          <p class="contacts-title">Связаться</p>
          <div class="contact-row">
            <a href="mailto:s501671@niuitmo.ru" class="contact-icon-btn mail-icon-btn">
              <img src="/assets/logos/mail.png" alt="Email" />
            </a>
            <a href="mailto:s501671@niuitmo.ru">s501671@niuitmo.ru</a>
          </div>
          <div class="contact-row">
            <a href="https://t.me/sharky_a" target="_blank" rel="noopener noreferrer" class="contact-icon-btn tg-icon-btn">
              <img src="/assets/logos/tg.svg" alt="Telegram" />
            </a>
            <a href="https://t.me/sharky_a" target="_blank" rel="noopener noreferrer">@sharky_a</a>
          </div>
        </div>

        <div class="info-footer">🤖 Создано с использованием ИИ • v{SITE_VERSION}</div>
      </section>
    </main>

    <style>
      :root {
        --bg-dark: #0f1310;
        --khaki: #8b9d6f;
        --khaki-dark: #556b2f;
        --paper: #e9dfc7;
        --ink: #1c1c1c;
        --muted: #c7c0ae;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        background: var(--bg-dark);
        color: #f4f1e8;
      }

      a {
        color: inherit;
      }

      .page-shell {
        min-height: 100vh;
        display: flex;
        flex-direction: row;
        background: radial-gradient(circle at 20% 20%, rgba(139, 157, 111, 0.12), transparent 35%),
          radial-gradient(circle at 80% 10%, rgba(85, 107, 47, 0.15), transparent 40%),
          linear-gradient(135deg, #0f1310 0%, #11160f 60%, #0c0f0c 100%);
      }

      .split {
        width: 50%;
        min-height: 100vh;
        position: relative;
      }

      .hero-panel {
        background-image: linear-gradient(120deg, rgba(12, 15, 12, 0.7), rgba(12, 15, 12, 0.3)),
          radial-gradient(circle at 30% 30%, rgba(255, 255, 255, 0.05), transparent 45%),
          repeating-linear-gradient(45deg, rgba(255, 255, 255, 0.04) 0, rgba(255, 255, 255, 0.04) 2px, transparent 2px, transparent 6px),
          linear-gradient(135deg, #4a4a42 0%, #2f2d27 40%, #1d1b16 100%);
        background-size: cover;
        background-position: center;
        display: grid;
        place-items: center;
        isolation: isolate;
      }

      .hero-overlay {
        width: min(520px, 80%);
        padding: 38px 34px;
        border: 1px solid rgba(255, 255, 255, 0.18);
        background: linear-gradient(120deg, rgba(24, 30, 22, 0.8), rgba(18, 22, 18, 0.6));
        backdrop-filter: blur(4px);
        border-radius: 14px;
        box-shadow: 0 22px 50px rgba(0, 0, 0, 0.4);
        text-align: center;
      }

      .hero-kicker {
        margin: 0 0 10px 0;
        letter-spacing: 0.16em;
        text-transform: uppercase;
        font-size: 13px;
        color: var(--muted);
      }

      .hero-title {
        margin: 0;
        font-family: "Roboto Slab", serif;
        font-size: 40px;
        letter-spacing: 0.08em;
        color: #f8f5ec;
        text-shadow: 0 6px 18px rgba(0, 0, 0, 0.55);
      }

      .hero-subtitle {
        margin: 10px 0 24px 0;
        font-size: 16px;
        opacity: 0.95;
        letter-spacing: 0.08em;
        color: #ede8db;
      }

      .cta-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 14px 28px;
        background: linear-gradient(135deg, #74835a 0%, #556b2f 100%);
        color: #0b0e0b;
        font-weight: 700;
        letter-spacing: 0.05em;
        border-radius: 999px;
        text-decoration: none;
        box-shadow: 0 12px 26px rgba(0, 0, 0, 0.35);
        border: 1px solid rgba(255, 255, 255, 0.18);
        transition: transform 0.2s ease, box-shadow 0.2s ease;
      }

      .cta-button:hover {
        transform: translateY(-2px) scale(1.01);
        box-shadow: 0 16px 30px rgba(0, 0, 0, 0.42);
      }

      .info-panel {
        background: linear-gradient(180deg, rgba(18, 21, 18, 0.95) 0%, rgba(12, 14, 12, 0.98) 100%);
        padding: 40px 48px;
        display: flex;
        flex-direction: column;
        gap: 22px;
      }

      .info-top {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 18px;
      }

      .logo-wrap {
        background: rgba(255, 255, 255, 0.04);
        border: 1px solid rgba(255, 255, 255, 0.12);
        padding: 10px 14px;
        border-radius: 10px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 10px 24px rgba(0, 0, 0, 0.25);
        position: relative;
        cursor: pointer;
        perspective: 1000px;
        min-width: 160px;
        min-height: 60px;
      }

      .logo-itmo {
        height: 40px;
        width: auto;
        display: block;
        animation: pulse 1s ease-in-out infinite;
        transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        filter: drop-shadow(0 4px 12px rgba(139, 157, 111, 0.4));
      }

      @keyframes pulse {
        0%, 100% {
          filter: drop-shadow(0 4px 12px rgba(139, 157, 111, 0.5)) brightness(1);
          transform: scale(1);
        }
        50% {
          filter: drop-shadow(0 8px 28px rgba(139, 157, 111, 0.9)) brightness(1.25);
          transform: scale(1.03);
        }
      }

      .logo-wrap::before {
        content: "Автор: ИСУ 501671";
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%) rotateY(180deg);
        opacity: 0;
        font-size: 13px;
        font-weight: 700;
        color: #F7F5EF;
        white-space: nowrap;
        pointer-events: none;
        letter-spacing: 0.05em;
        text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
        transition: opacity 0.3s ease 0.15s;
      }

      .logo-wrap:hover .logo-itmo {
        transform: rotateY(180deg);
        opacity: 0;
      }

      .logo-wrap:hover::before {
        transform: translate(-50%, -50%) rotateY(0deg);
        opacity: 1;
      }

      .sources-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        padding: 14px 20px;
        border-radius: 14px;
        background-image:
          linear-gradient(135deg, rgba(18, 21, 18, 0.72), rgba(12, 14, 12, 0.82)),
          url("/assets/logos/phozhito-logo.png");
        background-size: cover;
        background-position: center;
        background-blend-mode: multiply;
        border: 1px solid rgba(255, 255, 255, 0.2);
        text-decoration: none;
        color: #F7F5EF;
        font-weight: 800;
        letter-spacing: 0.05em;
        position: relative;
        overflow: hidden;
        box-shadow: 0 12px 28px rgba(0, 0, 0, 0.35);
      }

      .sources-button::after {
        content: "";
        position: absolute;
        inset: 0;
        background: radial-gradient(circle at 25% 25%, rgba(255, 255, 255, 0.12), transparent 45%);
        opacity: 0.7;
        pointer-events: none;
      }

      .sources-logo {
        display: none;
      }

      .sources-text {
        position: relative;
        z-index: 1;
        text-shadow: 0 3px 10px rgba(0, 0, 0, 0.4);
      }

      .info-card {
        background: radial-gradient(circle at 15% 20%, rgba(233, 223, 199, 0.18), transparent 55%),
          linear-gradient(120deg, rgba(31, 28, 22, 0.95), rgba(24, 22, 18, 0.92));
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 14px;
        padding: 24px 22px;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.05), 0 16px 36px rgba(0, 0, 0, 0.35);
      }

      .info-card-title {
        margin: 0 0 12px 0;
        font-size: 18px;
        letter-spacing: 0.04em;
        text-transform: uppercase;
      }

      .info-card-body {
        margin: 0;
        line-height: 1.6;
        color: #e5e0d4;
      }

      .contacts-card {
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.04), rgba(255, 255, 255, 0.02));
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 12px;
        padding: 18px 18px 14px;
        display: flex;
        flex-direction: column;
        gap: 10px;
      }

      .contacts-title {
        margin: 0;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        font-size: 13px;
        color: var(--muted);
      }

      .contact-row {
        display: flex;
        align-items: center;
        gap: 10px;
        font-weight: 600;
      }

      .contact-row a {
        text-decoration: none;
        color: #f5f3ea;
      }

      .contact-row a:hover {
        text-decoration: underline;
      }

      .contact-icon-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 24px;
        height: 24px;
        border-radius: 4px;
        background: rgba(255, 255, 255, 0.06);
        border: 1px solid rgba(255, 255, 255, 0.12);
        flex-shrink: 0;
        transition: all 0.2s ease;
      }

      .contact-icon-btn img {
        width: 14px;
        height: 14px;
        display: block;
        filter: brightness(0.95);
      }

      .contact-icon-btn:hover {
        background: rgba(255, 255, 255, 0.12);
        border-color: rgba(255, 255, 255, 0.2);
        transform: scale(1.1);
      }

      .contact-icon-btn:hover img {
        filter: brightness(1.1);
      }

      .contact-buttons {
        font-size: 16px;
        opacity: 0.85;
      }

      .contact-buttons {
        display: flex;
        gap: 42px;
        margin-top: 44px;
        padding-top: 42px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
      }

      .contact-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 44px;
        height: 44px;
        border-radius: 10px;
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.15);
        text-decoration: none;
        transition: all 0.3s ease;
        cursor: pointer;
      }

      .contact-btn img {
        width: 24px;
        height: 24px;
        display: block;
        filter: brightness(0.95) drop-shadow(0 1px 3px rgba(0, 0, 0, 0.3));
      }

      .contact-btn:hover {
        background: rgba(255, 255, 255, 0.15);
        border-color: rgba(255, 255, 255, 0.25);
        transform: translateY(-2px);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.25);
      }

      .contact-btn:hover img {
        filter: brightness(1.1) drop-shadow(0 2px 6px rgba(0, 0, 0, 0.4));
      }

      .info-footer {
        margin-top: auto;
        padding: 12px 16px;
        border-radius: 10px;
        background: rgba(0, 0, 0, 0.45);
        border: 1px solid rgba(255, 255, 255, 0.08);
        font-size: 13px;
        letter-spacing: 0.03em;
      }

      @media (max-width: 1024px) {
        .page-shell {
          flex-direction: column;
        }

        .split {
          width: 100%;
          min-height: 60vh;
        }

        .info-panel {
          min-height: auto;
        }
      }

      @media (max-width: 640px) {
        .hero-overlay {
          padding: 26px 22px;
        }

        .hero-title {
          font-size: 32px;
        }

        .hero-subtitle {
          font-size: 15px;
        }

        .info-panel {
          padding: 28px 22px;
        }

        .info-top {
          flex-direction: column;
          align-items: flex-start;
          gap: 12px;
        }

        .sources-button {
          width: 100%;
          justify-content: center;
        }

        .logo-itmo {
          height: 34px;
        }
      }
    </style>
  </body>
</html>
